import { Alert, Box, Button, Card, CardContent, Stack, TextField, Typography } from '@mui/material'
import QrCode2RoundedIcon from '@mui/icons-material/QrCode2Rounded'
import type { BookingDraft } from '../../types/rental'
type Props = { draft: BookingDraft; onCancel: () => void; onConfirm: () => void }
export function PaymentPage({ draft, onCancel, onConfirm }: Props) { const amount = draft.car.price; return <Stack spacing={2}><Typography variant="h5" sx={{ fontWeight: 900 }}>ชำระเงิน</Typography><Alert severity="info">ยอดชำระ ฿{amount.toLocaleString()} ถูกล็อกไว้สำหรับรายการนี้</Alert><Card><CardContent><Stack spacing={1} sx={{ alignItems: 'center' }}><QrCode2RoundedIcon sx={{ fontSize: 180 }} color="primary" /><Typography sx={{ fontWeight: 800 }}>PromptPay QR</Typography><Button size="small" onClick={() => window.print()}>บันทึก QR Code</Button></Stack></CardContent></Card><Card><CardContent><Typography sx={{ fontWeight: 800 }}>โอนผ่านบัญชีธนาคาร</Typography><Typography>บริษัท เช่ารถขับเอง จำกัด</Typography><Typography variant="h6">123-4-56789-0</Typography><Typography color="text.secondary">ธนาคารกสิกรไทย</Typography></CardContent></Card><TextField label="แนบรูปภาพสลิปโอนเงิน" type="file" slotProps={{ inputLabel: { shrink: true } }} /><Box sx={{ display: 'flex', gap: 1 }}><Button fullWidth variant="outlined" color="inherit" onClick={onCancel}>ยกเลิก</Button><Button fullWidth variant="contained" onClick={onConfirm}>ยืนยันชำระเงิน</Button></Box></Stack> }
